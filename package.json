{
  "scripts": {
    "test": "python -m unittest discover -s tests -p \"test_*.py\"",
    "api": "python -m uvicorn app.main:app --port 8000 --app-dir ai-service"
  },
  "dependencies": {
    "firebase": "^12.12.0"
  }
}
